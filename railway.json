{
  "$schema": "https://railway.app/json-schema/v0/railroad.json",
  "build": {
    "builder": "nixpacks",
    "buildCommand": "cd server && npm install"
  },
  "deploy": {
    "startCommand": "cd server && npm start",
    "restartPolicyType": "on_failure",
    "restartPolicyMaxRetries": 5
  }
}
