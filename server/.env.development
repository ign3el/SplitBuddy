# Development/Test Environment Configuration
# NOTE: Update DB_PASSWORD with actual credentials for database testing
# For now using placeholders - backend will run but DB operations will fail gracefully

# Database (update password from Aiven dashboard)
DB_HOST=ign3el-mysql-01-splitbuddy-ign3el.k.aivencloud.com
DB_PORT=11497
DB_USER=avnadmin
DB_PASSWORD=update_from_aiven_dashboard
DB_NAME=defaultdb
DB_SSL_REQUIRED=true

# JWT Secret (use a different secret for dev)
JWT_SECRET=dev_jwt_secret_for_local_testing_only

# CORS - Allow local development
CORS_ORIGIN=http://localhost:8080

# Mock email for development (don't send real emails)
MOCK_EMAIL=true

# SMTP (not needed when MOCK_EMAIL=true)
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
SMTP_SECURE=false

# Local development URLs
FRONTEND_URL=http://localhost:8080
RESET_PUBLIC_URL=http://localhost:3003
VERIFY_PUBLIC_URL=http://localhost:3003

# Server port
PORT=3003
NODE_ENV=development

